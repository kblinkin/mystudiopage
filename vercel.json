{
  "rewrites": [
    {
      "source": "/(.*)",
      "has": [
        {
          "type": "host",
          "value": "(?<subdomain>(?!www)[^.]+)\\.mystudiopage\\.com"
        }
      ],
      "destination": "/api/serve-site?s=$subdomain"
    }
  ]
}
